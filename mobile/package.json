{
  "name": "@videoediting/mobile",
  "version": "1.0.0",
  "private": true,
  "description": "ViralEdit AI — Capacitor native mobile shell wrapping the Next.js app",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "android:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "android:release": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
